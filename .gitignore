# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# ---- Private working material — NEVER publish (raw photos, notes, drafts) ----
# 3 Sep 2026: the loose reference folders (design by aman/, website data/,
# My Design Guidelines/) were consolidated into reference/{pixel,shared-projects,
# design-language}. google stitch data/ and design-tests/ were deleted.
reference/
.claude/
/HANDOFF.md
Codex Image v8
# Local content-editing working docs (mirror src/content/projects — edited
# locally and shared via chat, not a second source of truth in git).
/AI-PROJECTS-CONTENT.md
/AI-portfolio-case-study-pack.md
public/hero/_backup_avatar/
public/header-variants.html
BUILD-SPEC.md
CONTENT-INVENTORY.md
README.md

# Paper theme: source art originals are huge (127MB). Keep them local only.
# Web-ready copies are served from public/paper/ (committed).
Paper/References/

# 4 Sep 2026: Paper/assets/ is a local pipeline output (process-assets.mjs
# writes web-ready copies to BOTH Paper/assets/ and public/paper/). The build
# only serves public/paper/, so the committed Paper/assets/ copy was 13M of
# pure duplication on GitHub. Untracked + ignored; local copy stays.
Paper/assets/

# Paper theme: raw font files Aman dropped in (19 Aug 2026). Several of
# these (Apple's SF Pro/SF Mono/New York, the commercial Euclid Circular B)
# are not cleared for redistribution — never let the originals into git
# history regardless of what gets self-hosted. Converted woff2 web copies
# of the actually-licensed fonts live in public/fonts/ and ARE committed.
Paper/Fonts/
